# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /*path                           controllers.Application.loginProxy(path)
POST        /*path                           controllers.Application.loginProxy(path)
PUT         /*path                           controllers.Application.loginProxy(path)
